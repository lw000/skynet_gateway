include "config.path"
 
thread = 8
-- 启动的第一服务及参数，默认为snlua bootstrap。
-- 表示启动名为bootstrap的Lua服务，即service/bootstrap.lua。
bootstrap = "snlua bootstrap"
 
harbor = 0

-- main script
start = "main"

-- logger
--logger = "./log/skynet.log"
logservice = "logger"
logpath = "."
 
-- daemon
--daemon = "skynet.pid"

--address = "0.0.0.0:10001"
--master = "0.0.0.0:10000"
--standalone = "0.0.0.0:10000"